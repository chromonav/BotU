<nav class="navbar navbar-light bg-light">
  <a class="navbar-brand" href="#">
        <img src="/images/chatbot.png" class="d-inline-block align-top" alt="">
        UBot
      </a>

  <div class="navbar-right">
      {{#if isSession}}
          <span>
              <a href="/signout"><button class="btn btn-default btn-lg">Logout</button></a>
          </span>

      {{else}}
          <span>
              <a href="/signin"><button class="btn btn-default btn-lg">Signin</button></a>
          </span>

      {{/if}}

  </div>

  <button class="tog" onclick="tog()">&#9776;</button> {{#if isSession}}

  <div class="hid" id="na">
    <br>
    <div class="ah"><a href="/signout"><button href="/signout">Logout</button></a></div>
  </div>

  {{else}}

  <div class="hid" id="na">
    <br>
    <div class="ah"><a href="/signin"><button >Signin</button></a></div>
  </div>
  {{/if}}
</nav>
